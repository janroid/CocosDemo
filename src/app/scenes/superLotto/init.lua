
-- 数据包，init中声明外部可访问的信息
local init = {
   	SuperLottoPop = require("SuperLottoPop");
   	SuperLottoRulePop = require("SuperLottoRulePop");
   	SuperLottoRewardPop = require("SuperLottoRewardPop");
   	SuperLottoRewardListPop = require("SuperLottoRewardListPop");
}

return init
