local tab = {
    DB = require("base.DB");
    AccountInfo = require("AccountInfo");
 	AccountManager = require("AccountManager");
    PhpInfo = require("PhpInfo");
    RoomInfo = require("RoomInfo");
    Model = require('base.Model');
    ModelCmd = require('base.ModelCmd');
    SlotInfo = require("SlotInfo");
    AchieveInfo = require("AchieveInfo");
}

return tab