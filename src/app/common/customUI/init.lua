return {
    TabarView = require("TabarView");
    AlarmTips = require("AlarmTips");
    NetImageView = require("NetImageView");
    RefreshListView = require("RefreshListView");
    AlertDialog = require("AlertDialog");
    Progress = require("Progress");
    NotificationNode = require("NotificationNode");
    RectTimer = require("RectTimer");
    BroadcastNotify = require("BroadcastNotify");
    TopTips = require("TopTips");
    MarqueeText = require("MarqueeText");
    SpriteBlur = require("SpriteBlur");
}